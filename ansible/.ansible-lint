---
# ansible-lint configuration.
#
# Role discovery: ansible-lint resolves roles relative to the playbook directory
# (ansible/playbooks/roles) by default. This config points to the tree root.
profile: production

exclude_paths:
  - .github/

# Treat this directory as the project root for role resolution.
kinds:
  - playbook: "playbooks/*.yml"
