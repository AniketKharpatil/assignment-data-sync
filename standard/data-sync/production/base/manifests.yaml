---
# Source: data-sync/templates/pdb.yaml
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: data-sync
  labels:
    helm.sh/chart: data-sync-0.1.0
    app.kubernetes.io/name: data-sync
    app.kubernetes.io/instance: data-sync
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: data-sync
    environment: production
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: data-sync
      app.kubernetes.io/instance: data-sync
---
# Source: data-sync/templates/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: data-sync
  labels:
    helm.sh/chart: data-sync-0.1.0
    app.kubernetes.io/name: data-sync
    app.kubernetes.io/instance: data-sync
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: data-sync
    environment: production
data:
  APP_ENV: "production"
  LOG_LEVEL: "INFO"
  WORKERS: "4"
  MAX_CONNECTIONS: "100"
  REDIS_HOST: "data-sync-prod.xyz789.ng.0001.aps1.cache.amazonaws.com"
  REDIS_PORT: "6379"
---
# Source: data-sync/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: data-sync
  labels:
    helm.sh/chart: data-sync-0.1.0
    app.kubernetes.io/name: data-sync
    app.kubernetes.io/instance: data-sync
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: data-sync
    environment: production
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: data-sync
    app.kubernetes.io/instance: data-sync
---
# Source: data-sync/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-sync
  labels:
    helm.sh/chart: data-sync-0.1.0
    app.kubernetes.io/name: data-sync
    app.kubernetes.io/instance: data-sync
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: data-sync
    environment: production
spec:
  revisionHistoryLimit: 5
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 0
    type: RollingUpdate
  selector:
    matchLabels:
      app.kubernetes.io/name: data-sync
      app.kubernetes.io/instance: data-sync
  template:
    metadata:
      annotations:
        checksum/config: d16649618af629cf1906f83adaa9b58c2f02e2a734726877be7f04ba062e0184
      labels:
        app.kubernetes.io/name: data-sync
        app.kubernetes.io/instance: data-sync
    spec:
      automountServiceAccountToken: false
      priorityClassName: platform-high
      terminationGracePeriodSeconds: 30
      securityContext:
        fsGroup: 10001
        runAsGroup: 10001
        runAsNonRoot: true
        runAsUser: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: data-sync
          image: ghcr.io/example/data-sync:1.0.0
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            readOnlyRootFilesystem: true
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: data-sync
          env:
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: data-sync-redis
                  key: REDIS_PASSWORD
          startupProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 3
            timeoutSeconds: 2
            failureThreshold: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
            successThreshold: 1
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 6
            successThreshold: 1
          resources:
            limits:
              cpu: 2000m
              memory: 1Gi
            requests:
              cpu: 1000m
              memory: 1Gi
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 5"]
      volumes:
        - name: tmp
          emptyDir: {}
      topologySpreadConstraints:
        - labelSelector:
            matchLabels:
              app.kubernetes.io/instance: data-sync
              app.kubernetes.io/name: data-sync
          maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: DoNotSchedule
        - labelSelector:
            matchLabels:
              app.kubernetes.io/instance: data-sync
              app.kubernetes.io/name: data-sync
          maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
      nodeSelector:
        workload: data-sync
      tolerations:
        - effect: NoSchedule
          key: dedicated
          operator: Equal
          value: data-sync
---
# Source: data-sync/templates/hpa.yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: data-sync
  labels:
    helm.sh/chart: data-sync-0.1.0
    app.kubernetes.io/name: data-sync
    app.kubernetes.io/instance: data-sync
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: data-sync
    environment: production
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: data-sync
  minReplicas: 3
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
  behavior:
    scaleDown:
      policies:
      - periodSeconds: 120
        type: Percent
        value: 25
      stabilizationWindowSeconds: 600
    scaleUp:
      policies:
      - periodSeconds: 15
        type: Percent
        value: 100
      - periodSeconds: 15
        type: Pods
        value: 4
      selectPolicy: Max
      stabilizationWindowSeconds: 0
---
# Source: data-sync/templates/servicemonitor.yaml
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: data-sync
  labels:
    helm.sh/chart: data-sync-0.1.0
    app.kubernetes.io/name: data-sync
    app.kubernetes.io/instance: data-sync
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: data-sync
    environment: production
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: data-sync
      app.kubernetes.io/instance: data-sync
  namespaceSelector:
    matchNames:
      - data-sync
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
      scheme: http
